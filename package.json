{
  "name": "@emir-tickets/common",
  "version": "1.0.1",
  "description": "",
  "main": "./build/index.js",
  "types": "./build.index.d.ts",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "clean": "del ./build/*",
    "build": "npm run clean && tsc",
    "pub": "git add . && git commit -m \"Updates\" && npm version patch && npm run build && npm publish"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "del-cli": "^5.0.0",
    "typescript": "^4.8.4"
  },
  "//comment": "Command pub is not a way that it should be done in real project. Commands like git add ., git commit -m and npm version patch, are questionable"
}
